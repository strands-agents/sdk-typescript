# AWS credentials for Bedrock (required for the swarm to call the model).
# Copy to .env and fill in. Do not commit .env.
#
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...
# AWS_REGION=us-west-2
#
# Optional: use a specific region (default is us-west-2).
# AWS_REGION=us-east-1
#
# Optional: enable OpenTelemetry capture in memory for the Logs panel.
# OTEL_ENABLED=1
# Optional: print every span to server stdout (very noisy; disabled by default).
# OTEL_CONSOLE_EXPORT=1

# Optional: hard run token budget. Server aborts runs that exceed this total.
# MAX_RUN_TOTAL_TOKENS=100000
# Optional: hard wall-clock limit per run in milliseconds.
# MAX_RUN_WALL_CLOCK_MS=300000
# Optional: kill a run if no stream event arrives within this window (milliseconds).
# MAX_STREAM_IDLE_MS=60000

# Optional: sqlite database path for run history persistence.
# Defaults to ./examples/browser-multi-agent-stream/.data/browser-multi-agent-stream.sqlite
# RUN_HISTORY_DB_PATH=/absolute/path/to/runs.sqlite
